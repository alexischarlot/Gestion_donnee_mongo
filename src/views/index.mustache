{{> header}}
    <a href="/dep?start={{start}}&end={{end}}">dep{{start}}</a><button><i class="fas fa-map-marker-alt"></i></button>
    <a href="/ar?start={{start}}&end={{end}}">{{end}}</a><button><i class="fas fa-map-marker-alt"></i></button>
    <div id="map"></div>
    <script>
        // Initialize and add the map
        function initMap() {
          // The location of Uluru
          var uluru = {lat: 48.692054, lng: 6.184416999999939};
          // The map, centered at Uluru
          var map = new google.maps.Map(
              document.getElementById('map'), {zoom: 4, center: uluru});
          // The marker, positioned at Uluru
          var marker = new google.maps.Marker({position: uluru, map: map});
        }
            </script>
{{> footer}}